# Solutions C++

Ce dossier contient une proposition complète pour les exercices visibles sur les images.

## Structure

- `TP1/` : implémentation de la classe `Rectangle` demandée dans le premier sujet, plus une variante basée sur deux objets `Point` pour l'exercice 17.
- `TP2/` : implémentation du diagramme de classes (`Point`, `Forme`, `Triangle`, `Quadri`, `Cercle`) du deuxième sujet.

## Compilation

Pour compiler chaque TP :

```bash
cd /home/yasser/TP_saddoune/solutions/TP1 && make
cd /home/yasser/TP_saddoune/solutions/TP2 && make
```

## Exécution

```bash
/home/yasser/TP_saddoune/solutions/TP1/tp1
/home/yasser/TP_saddoune/solutions/TP2/tp2
```
